import React from 'react';

function Parent() {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        fontSize: '40px',
        height: '90vh',
      }}
    >
      Parent Page
    </div>
  );
}

export default Parent;
