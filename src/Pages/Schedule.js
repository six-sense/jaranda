import React from 'react';

function Schedule() {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        fontSize: '40px',
        height: '90vh',
      }}
    >
      Schedule Page
    </div>
  );
}

export default Schedule;
